extends ../layout

block content
	.container
		.row
			ul.collection.with-header
				li.collection-header
					h5.font_size_22 通知一覧

				if notifications.length != 0   	
					for	notif in notifications
						li.collection-item
							a(href="/#{notif.model_name}/#{notif.model_id}")= notif.message
				else
					li.collection-item 
						a(href="/") 新しい通知はまだありません(・∀・)！！
