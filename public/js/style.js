// Generated by CoffeeScript 1.9.3
(function() {
  $(function() {
    var card_box, h, load_more, loading, parallax, parallax_height, parallax_img, w, wrf;
    $('.parallax').parallax();
    $(".dropdown-button").dropdown();
    $(".button-collapse").sideNav();
    $('.modal-trigger').leanModal();
    $(".dropdown-button").dropdown();
    $('.materialboxed').materialbox();
    $('.datepicker').pickadate({
      selectMonths: true,
      selectYears: 15
    });
    w = $(window);
    h = w.height();
    $("#before_login_page").css({
      height: h
    });
    wrf = $(".show_write_rep");
    wrf.on("click", function() {
      var form_id, this_id;
      this_id = $(this).attr("id");
      form_id = this_id + "_form";
      return $("#" + form_id).slideToggle();
    });
    w = $(window);
    parallax = $(".parallax-container");
    parallax_height = parallax.height() * 0.666;
    parallax_img = $(".parallax img");
    w.scroll(function() {
      var sc;
      sc = $(this).scrollTop();
      if (parallax_height < sc) {
        return parallax_img.addClass("blur");
      } else {
        return parallax_img.removeClass("blur");
      }
    });
    loading = $("#loading");
    load_more = $("#load_more");
    loading.hide();
    card_box = $("#card_box");
    return load_more.on("click", function() {
      var page_id;
      $(this).hide();
      loading.show();
      page_id = $(".page_id").attr("id");
      return $.post("/posts/more", {
        page_id: page_id
      }, function(data) {
        var i, id, len, post, post_card, ref;
        id = data.page_id;
        $(".page_id");
        ref = data.posts;
        for (i = 0, len = ref.length; i < len; i++) {
          post = ref[i];
          post_card = "<article id='posted_card_" + post.id + "' class='col s12 m6 l4'> <div class='card'> <div class='card-content'> <div class='row'> <div class='col s2 m3 l2'><img src='" + (post.user_image != null ? post.user_image : "/images/amethyst_flat.png") + "' class='circle responsive-img'></div> <div class='col s10 m9 l10'><span class='card-title cyan-text'>" + post.user_name + "</span></div> </div> <p>" + (post.body.replace(/\n/g, '<br/>')) + "</p> <span class='font_size_10 right'>" + post.created_at + "</span> </div> <div class='card-action'><a href='/posts/" + post.id + "' class='teal-text'>コメント</a></div> </div> </article>";
          card_box.prepend(post_card);
        }
        loading.hide();
        return load_more.fadeIn();
      });
    });
  });

}).call(this);
